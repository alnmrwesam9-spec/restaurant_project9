*** Begin Patch
*** Update File: core/admin.py
@@
-from django.contrib import admin
+from django.contrib import admin
+from django import forms
@@
 @admin.register(Dish)
 class DishAdmin(admin.ModelAdmin):
@@
     readonly_fields = ("display_codes", "codes_updated_at")
@@
     actions = (
         action_enable_manual_codes,
         action_disable_manual_codes,
         action_copy_generated_to_manual,
         action_clear_manual_codes,
     )
+
+    # Use a custom form to present a single unified "codes" field in admin
+    class DishAdminForm(forms.ModelForm):
+        codes = forms.CharField(
+            label=_("Codes"), required=False,
+            help_text=_("Single field for allergen letters A..R, comma-separated (e.g. A,G,K)."),
+        )
+
+        class Meta:
+            model = Dish
+            fields = "__all__"
+
+        def __init__(self, *args, **kwargs):
+            super().__init__(*args, **kwargs)
+            inst = self.instance
+            initial_codes = ""
+            if inst and getattr(inst, "pk", None):
+                # Prefer manual codes; otherwise fall back to generated
+                initial_codes = (getattr(inst, "manual_codes", "") or getattr(inst, "generated_codes", "") or "").strip()
+            self.fields["codes"].initial = initial_codes
+
+            # Hide low-level fields to avoid duplication
+            for f in ("generated_codes", "manual_codes", "has_manual_codes"):
+                if f in self.fields:
+                    self.fields[f].widget = forms.HiddenInput()
+                    self.fields[f].required = False
+
+        def clean_codes(self):
+            import re
+            raw = (self.cleaned_data.get("codes") or "").upper()
+            letters = re.findall(r"[A-R]", raw)
+            seen = set(); out = []
+            for c in letters:
+                if c not in seen:
+                    seen.add(c); out.append(c)
+            return ",".join(out)
+
+        def save(self, commit=True):
+            codes = (self.cleaned_data.get("codes") or "").strip()
+            # Treat this single field as source of truth for admin edits
+            self.instance.manual_codes = codes or None
+            self.instance.has_manual_codes = True  # keep manual mode, even if empty -> intentionally no codes
+            # Keep legacy field in sync for any old displays
+            self.instance.allergy_info = codes
+            return super().save(commit)
+
+    form = DishAdminForm
+
+    # Exclude low-level fields from the displayed form (managed by the custom form)
+    exclude = ("generated_codes", "manual_codes", "has_manual_codes")
*** End Patch
